# 🚑 دليل الطوارئ — لو قاعدة البيانات أو الاستضافة ماتت

النظام دلوقتي فيه **حماية ذاتية من الانهيار** — الحالات بيتعالج معظمها تلقائياً.
الملف ده بيشرح إيه اللي بيحصل لوحده، وإيه اللي ممكن تحتاجه يدوياً في أسوأ حالة.

---

## 🟢 إيه اللي بيشتغل لوحده (من غير تدخل أي حد)

### 1) نسخ احتياطي متبادل كل 30 دقيقة
كل سيرفر (gamma/kappa/dun/seven) بيعمل **لقطة كاملة** من كل بياناته ويرفعها على
السيرفرات التلاتة التانيين (بتتخزن في قواعدهم هي). يعني أي سيرفر شايل نسخة
كاملة من التلاتة التانيين — على استضافات وقواعد مستقلة تماماً.
- المشغّل: GitHub Actions وركفلو `DB Cluster Sync` كل 30 دقيقة.
- يمكن تشغيلها يدوياً من لوحة السوبر أدمن → كارت «🛡️ الحماية» → ⚡ مزامنة.

### 2) قلب تلقائي على قاعدة احتياطية (Failover)
لو القاعدة الأساسية ماتت (زي ما حصل مع ليالينا):
- أول طلب هيكتشف الموت تلقائياً (P1001/انقطاع اتصال).
- بيجرّب القواعد الاحتياطية المسجلة بالترتيب: `STANDBY_DB_URLS` في env
  + القواعد المسجلة من لوحة السوبر أدمن.
- لو القاعدة الاحتياطية **فاضية من غير سكيما** → بيزرع السكيما كاملة لوحده.
- لو **فاضية بيانات** → بيسحب آخر لقطة محفوظة على باقي السيرفرات ويرجّعها لوحده.
- بعدها النظام شغال طبيعي 100% — الكروت والدخول واللوحات كلها.

### 3) لو القاعدة الأساسية رجعت
النظام يفضل شغال على الاحتياطية. الرجوع للمرة الأولى يدوي من اللوحة
(زر 🔁 تحويل) أو بتحديث `DATABASE_URL` — والبيانات بتتزامن عكسياً بضغطة
♻️ استرجاع قبل التحويل.

---

## 🟡 سيناريو: قاعدة السيرفر ماتت نهائياً (زي واقفة على Neon)

1. اعمل قاعدة جديدة (Neon جديدة أو أي Postgres).
2. سجّل رابطها كقاعدة احتياطية من اللوحة: كارت 🛡️ → ➕ قاعدة → الصق الرابط.
   (النظام هيختبرها ويزرع السكيما لوحده)
3. اضغط 🔁 تحويل → النظام يقلب عليها ويسحب آخر نسخة من باقي السيرفرات.
4. حدّث `DATABASE_URL` في استضافة السيرفر بالرابط الجديد عشان يثبت بعد إعادة النشر.

## 🔴 سيناريو: الاستضافة نفسها ماتت (Vercel وقع/الباند اتقفل)

الريبو ده هو مصدر الحقيقة — شغّله على أي منصة تدعم Next.js:
- **Docker**: `docker build -t hotspot . && docker run -p 3000:3000 -e DATABASE_URL=<رابط أي قاعدة شغالة> hotspot`
- **Render**: موجود جاهز (`render.yaml`).
- أي قاعدة احتياطية/أساسية شغالة كفاية — السكيما والبيانات هيتموا لوحدهم عند أول تشغيل.
- بعدين وجّه الدومين/الراوتر للسيرفر الجديد (أو استخدم `/api/setup/migrate` لنقل الكافيه).

> 💡 خلي بالك: الراوترات بتتكلم مع السيرفر عبر الدومين — لو الدومين اتغير،
> شغّل سكربت التثبيت الجديد على الراوتر (بيصدره النظام) أو عدّل إعدادات wifidog.

---

## 🔐 أسرار مهمة (متخزنة في env على الاستضافة — مش في الريبو)

| المتغير | وظيفته |
|---|---|
| `DATABASE_URL` | القاعدة الأساسية |
| `STANDBY_DB_URLS` | قواعد احتياطية مفصولة بفواصل (اختياري — في اللوحة كفاية) |
| `CRON_SECRET` | سر المزامنة/الكرون (الافتراضي موجود في الكود — يفضّل تغييره + ضبطه في GitHub Variables باسم `DB_SYNC_SECRET`) |
| `SUPER_ADMIN_USERNAME/PASSWORD/EMAIL` | إنشاء السوبر أدمن أول تشغيل |

## 🧪 تجربة الحماية بنفسك (مزيف — بدون خطر)

1. افتح لوحة السوبر أدمن → كارت 🛡️ → ⚡ مزامنة على كل سيرفر.
2. اتأكد إن كل سيرفر بيورّي نسخ التلاتة التانيين.
3. جرّب ♻️ استرجاع على سيرفر منهم — المفروض يقول «تم الاسترجاع» بأعداد الصفوف.
